/**
 * This file contains all the routes used in the app
 * make sure to group them by the page they are used in
 */

const ROUTES = {
	// landing
	root: '/',
	//Edit Profile

	/**
	 * this is for external websites
	 */
	external: {
		example: 'https://example.com',
	},
};

export default ROUTES;
